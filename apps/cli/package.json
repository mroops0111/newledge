{
  "name": "@newledge/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "newledge": "./dist/index.js"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --coverage",
    "start": "node ./dist/index.js",
    "clean": "rm -rf dist .turbo *.tsbuildinfo coverage"
  },
  "dependencies": {
    "@braidhq/agent-claude-code": "^0.3.0",
    "@braidhq/core": "^0.3.0",
    "@braidhq/schema": "^0.3.0",
    "@braidhq/server": "^0.3.0",
    "@braidhq/storage-kuzu": "^0.3.0",
    "@hono/node-server": "^2.0.12",
    "@newledge/ontology-knowledge": "workspace:*",
    "@newledge/source-loader-web": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
